
@{
    ViewBag.Title = "审核学校";
    Layout = "~/Views/Shared/_LayoutCASAdmin.cshtml";
}

<h2>审核学校</h2>

<h2>四川农业大学</h2>

显示学校详情：
<ol>
    <li>学校名称</li>
    <li>所在地</li>
    <li>学校方代表人及联系方式</li>
    <li>授权合同材料（图片）</li>
    <li>等等...</li>
</ol>

<br />
@using (Html.BeginForm("SchAuditPass", "Admin", new { id = 2 }, FormMethod.Post, new { id = "auditForm" }))
{
    @Html.AntiForgeryToken()
    <div class="form-group">
        <label>不通过原因</label>
        <textarea name="reason" class="form-control" rows="2"></textarea>
    </div>
    <button type="submit" class="btn btn-success">通过</button>
    <button type="submit" onclick="javascript:$('#auditForm').attr('action','@Url.Action("SchAuditNotPass", "Admin", new { id = 2 })')" class="btn btn-danger">不通过</button>
}